  
  <div style="background-color: pink; margin-top: 25px">
  <div class="row">

<%=render 'shared/side_menu'%>
  <section class="col-sm-5 text-center" style="background-color: yellow">
    
    <h1>Receitas</h1>
    <% @recipes.each do |recipe|%>
      <h1><%= link_to recipe.title, recipe_path(recipe.id)%></h1>
      <li><%=recipe.recipe_type.name%></li>
      <li><%=recipe.cuisine.name %></li>
      <li><%=recipe.difficulty%></li>
      <li><%="#{recipe.cook_time} minutos"%></li>
    <%end%>

  </section>
  <section class="col-sm-5 text-center" style="background-color: gray">
    <h1>Favoritas</h1> <!-- nada por enquanto-->
  </section>


  </div>
  </div>

